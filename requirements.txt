# AI Persona — Full Requirements
# Install with: pip install -r requirements.txt

# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.3
pydantic-settings==2.3.1
python-multipart==0.0.9
python-dotenv==1.0.1
loguru==0.7.2
tenacity==8.4.2
httpx==0.27.0
email-validator==2.1.1      # required by pydantic EmailStr (optional, not used currently)


# GitHub
requests==2.32.3

# Embeddings + Vector Store
fastembed==0.3.6
chromadb==0.5.3

# LangChain
langchain==0.2.6
langchain-community==0.2.6
langchain-groq==0.1.6

# LLM
groq==0.9.0

# Frontend (installed separately via frontend/requirements.txt)
# streamlit is NOT needed on the Render backend server

# Voice (install separately if needed)
# livekit-agents==0.8.3
# livekit-plugins-deepgram==0.6.2
# livekit-plugins-elevenlabs==0.7.1

# Testing
pytest==8.2.2
pytest-asyncio==0.23.7
